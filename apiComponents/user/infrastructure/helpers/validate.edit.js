const validateEdit = (id, nameUser, lastNameUser, gender, documentType, documentNumber, address, email) => {
    //if(!id) throw new Error('Enter your id user')

    /*if(nameUser === undefined) nameUser = null
    if(passwordUser === undefined) passwordUser = null
    if(lastNameUser === undefined) lastNameUser = null
    if(gender === undefined) gender = null
    if(documentType === undefined) documentType = null
    if(documentNumber === undefined) documentNumber = null
    if(address === address) address = null
    if(email === undefined) email = null
    if(urlImg === undefined) urlImg = null*/

    //if(typeof nameUser !== "string")
    //    throw new Error('Invalid value for nameUser field')

}

module.exports = validateEdit